This is a demo application written to show how a Flex and AS3 based application can be written such that a shared
library contains the majority of code and multiple different projects can target different screen types (mobile, 
kiosk, web, etc.).